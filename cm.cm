<div class="container-fluid root" style="position: relative;">
  <div class="row">
    <div id="gridContainer" class="col-md-12 grid-container">
      <!-- Existing grid code -->
      <div #panthergridDiv></div>
    </div>
  </div>

  <!-- Conditionally display buttons based on showButtons value -->
  <div class="row" *ngIf="showButtons">
    <div class="col-md-12">
      <button (click)="toggleJiraIdsPopup()">Show JIRA IDS Popup</button>
      <button (click)="toggleUpstreamLineagePopup()">Show Upstream Lineage Popup</button>
    </div>
  </div>

  <!-- JIRA IDS Popup -->
  <div *ngIf="showJiraIdsPopup" class="popup">
    <div class="popup-content">
      <button class="close-btn" (click)="toggleJiraIdsPopup()">&times;</button>
      <h2>JIRA IDS Popup</h2>

      <!-- Render the JSON data if it exists -->
      <div *ngIf="jiraFieldData">
        <p><strong>Request ID:</strong> {{ jiraFieldData.meta.requestId }}</p>
        <p><strong>Requested System:</strong> {{ jiraFieldData.meta.requestedSystem }}</p>
        <p><strong>Status:</strong> {{ jiraFieldData.meta.status }}</p>
        <p><strong>JIRA ID:</strong> {{ jiraFieldData.data.jirald }}</p>
        <p><strong>Summary:</strong> {{ jiraFieldData.data.summary }}</p>
        <p><strong>Description:</strong> {{ jiraFieldData.data.description }}</p>
        <p><strong>Test Scenarios:</strong></p>
        <ul>
          <li *ngFor="let scenario of jiraFieldData.data.testScenarios">{{ scenario }}</li>
        </ul>
      </div>

      <!-- Show a loading message if data is still being fetched -->
      <div *ngIf="!jiraFieldData">
        <p>Loading JIRA data...</p>
      </div>
    </div>
  </div>

  <!-- Upstream Lineage Popup -->
  <div *ngIf="showUpstreamLineagePopup" class="popup">
    <div class="popup-content">
      <button class="close-btn" (click)="toggleUpstreamLineagePopup()">&times;</button>
      <h2>Upstream Lineage Popup</h2>
      <p>This is the content of the Upstream Lineage popup.</p>
    </div>
  </div>
</div>
